server:
  servlet:
    context-path: /api/v1
    application-display-name: persons
  forward-headers-strategy: framework
spring:
  servlet:
    multipart:
      resolve-lazily: true
  datasource:
    url: jdbc:postgresql://localhost:5432/persons
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
